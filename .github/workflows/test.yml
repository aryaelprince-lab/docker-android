name: Android Cloud

on:
  workflow_dispatch:

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Android Container
        run: |
          docker run -d -p 6080:6080 budtmo/docker-android:emulator_11
